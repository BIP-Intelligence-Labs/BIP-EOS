name: Documentation
on:
  push:
    paths:
      - "docs/**"
      - "engineering/**"
      - "README.md"
  workflow_dispatch:
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Validate documentation"
